import { {{ entity }} } from '@modules/{{ module }}/infra/typeorm/entities/{{ entity }}';

import { I{{ pascalCase pluralEntity }}Repository } from '../I{{ pascalCase pluralEntity }}Repository';

class InMemory{{ pascalCase pluralEntity }}Repository implements I{{ pascalCase pluralEntity }}Repository {
  private {{ camelCase pluralEntity }}: {{ entity }}[] = [];
}

export { InMemory{{ pascalCase pluralEntity }}Repository };
